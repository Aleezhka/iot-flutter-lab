name: workshop_app
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.3
  flutter_secure_storage: ^9.2.1
  provider: ^6.1.2
  mqtt_client: ^10.4.0
  connectivity_plus: ^6.0.3
  firebase_core: ^3.1.0
  firebase_auth: ^5.1.0
  google_sign_in: ^6.2.1
  dio: ^5.4.3
  flutter_bloc: ^9.1.1
  flashlight_plugin:
    git:
      url: https://github.com/Aleezhka/flashlight_plugin.git

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true